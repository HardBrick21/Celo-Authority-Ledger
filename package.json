{
  "name": "celo-authority-ledger",
  "version": "1.0.0",
  "scripts": {
    "deploy:testnet": "hardhat run scripts/deploy.js --network celo-alfajores",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network celo-mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  }
}